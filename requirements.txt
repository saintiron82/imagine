# ImageParser Dependencies (v0.6.1 Triaxis Architecture)

# PSD Parsing
psd-tools>=1.12.0
aggdraw>=1.4.0

# Image Processing
Pillow>=12.0.0
numpy>=2.0.0

# Data Validation
pydantic>=2.10.0

# File Watching
watchdog>=6.0.0

# Metadata Extraction (JPG/PNG EXIF)
exifread>=3.5.0

# CLI & Progress
tqdm>=4.67.0

# SQLite Vector Database (Triaxis VV/MV)
sqlite-vec>=0.1.6

# AI Models (SigLIP2, Qwen3-VL, Qwen3-Embedding)
torch>=2.6.0
transformers>=5.0.0
accelerate>=1.0.0

# Server (FastAPI)
fastapi>=0.115.0
uvicorn>=0.34.0
python-multipart>=0.0.20
bcrypt>=4.0.0

# HTTP Client (Ollama API, worker→server)
requests>=2.32.0

# Config
PyYAML>=6.0.0

# Translation
deep-translator>=1.11.0

# Environment Configuration
python-dotenv>=1.0.0

# System Monitoring (worker resource metrics)
psutil>=7.0.0

# MLX (Apple Silicon native acceleration, macOS only)
mlx>=0.22.0; sys_platform == 'darwin'
mlx-vlm>=0.3.0; sys_platform == 'darwin'

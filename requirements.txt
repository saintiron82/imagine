# ImageParser Dependencies (v0.6.1 Triaxis Architecture)

# PSD Parsing
psd-tools>=1.12.0
aggdraw>=1.4.0

# Image Processing
Pillow>=12.0.0
numpy>=2.0.0

# Data Validation
pydantic>=2.10.0

# File Watching
watchdog>=6.0.0

# Metadata Extraction (JPG/PNG EXIF)
exifread>=3.5.0

# CLI & Progress
tqdm>=4.67.0

# SQLite Vector Database (Triaxis VV/MV)
sqlite-vec>=0.1.6

# AI Models (SigLIP2, Qwen3-VL, Qwen3-Embedding)
torch>=2.6.0
transformers>=5.4.0
accelerate>=1.0.0

# Server (FastAPI)
fastapi>=0.115.0
uvicorn>=0.34.0
python-multipart>=0.0.20
bcrypt>=4.0.0
PyJWT>=2.9.0

# HTTP Client (Ollama API, worker→server)
requests>=2.32.0

# Config
PyYAML>=6.0.0

# Translation
deep-translator>=1.11.0

# Environment Configuration
python-dotenv>=1.0.0

# System Monitoring (worker resource metrics)
psutil>=7.0.0

# MLX (Apple Silicon native acceleration, macOS only)
mlx>=0.22.0; sys_platform == 'darwin'
mlx-vlm>=0.3.0; sys_platform == 'darwin'

# Firebase Auth (2-layer auth: Firebase identity + server password)
firebase-admin>=6.0.0

# ── Optional: agentcli ───────────────────────────────────────────────────
# Agentic CLI client — normalizes Codex/Claude/Copilot CLI sessions. Used by
# query_decomposer for Codex-based query understanding, and genuinely optional:
# query_decomposer.py:340 imports it in a try/except and falls back to the
# legacy subprocess path when it is missing.
#
# NOT listed as a hard requirement because it is not published on PyPI, so
# `pip install -r requirements.txt` failed outright on every machine without a
# local checkout — including CI, where it meant the pytest job never ran a
# single test. Install it explicitly if you want the session-aware Codex path:
#
#   pip install git+https://github.com/saintiron82/agentcli.git
#   # or, for local development:  pip install -e ../agentcli
